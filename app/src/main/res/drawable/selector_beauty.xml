<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 圆角设置 -->
    <corners
        android:radius="26dip"
        android:topLeftRadius="26dp"
        android:topRightRadius="26dp"
        android:bottomLeftRadius="26dp"
        android:bottomRightRadius="26dp"/>
    <!-- 边框宽度及颜色 -->
    <stroke android:width="2px" android:color="@color/btn_filled_blue_bg_pressed" />

    <!-- 空间内部填充
       <solid android:color="#E6E6FA" />
    -->


    <!-- 留空 -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
    <!-- 渐变色
    	startColor,开始的颜色，endcolor渐变最后的颜色,centerColor中间的颜色
		style有三种,linear（线性）,radial（放射性）,sweep（扫描性)
		linear从一个方向到另一个方向
		当angle=0时，渐变色是从左向右。
		然后渐变方向随angle增加而逆时针方向转，当angle=270时为从上往下,且只有type为center时有效 –>
		radial 从一个点开始的渐变
		android:centerX    Float.(0 - 1.0) 渐变点x轴相对位置。
		android:centerY!    Float.(0 - 1.0) 渐变点Y轴相对位置。
		这两个属性只有在type不为linear情况下起作用。
		android:useLevel="true| false"默认为false,且只有当其为false时才有渐变效果
     -->

    <!--
    <gradient
        android:startColor="#030303"：设置开始颜色
    android:type="radial":设置渐变放式为放射式
    type="linear"：平行式渐变
    type="sweep"：扫描渐变
    android:endColor="#fff000"：设置终止颜色
    android:gradientRadius="200%p"：设置渐变半径，该属性只有当type="radial"时有效
    android:centerColor="#2E8B57"/>：设置中间颜色
    -->
    <gradient
        android:startColor="#1227B1"
    android:type="radial"
    android:endColor="#00B3AEAE"
    android:gradientRadius="300%p"
    android:centerColor="#11DA83"/>
    <!-- 描边
     <stroke android:width="10dp"：描笔的高度(不是宽度)
    android:color="#ff00ff"：描笔的颜色
    android:dashWidth="20dp"：描笔的宽度
    android:dashGap="10dp"/>：相邻描笔之间的间隔
     -->
    <stroke android:width="5dp"
    android:color="@color/qmui_config_color_blue"
    android:dashWidth="10dp"
    android:dashGap="10dp"/>

</shape>